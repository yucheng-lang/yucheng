// 测试所有新开发的库

// === 测试 HTML生成 ===
导入 "HTML生成"
输出 "=== HTML生成库 ==="
设 页面 = HTML文档("测试页面", 段落("Hello 语程!") + 加粗("加粗文本") + 链接("https://example.com", "示例链接"))
输出 "HTML文档长度: " + 转文本(页面.长度())

设 表 = 表格([["A", "B"], ["1", "2"]], ["列1", "列2"])
输出 "表格长度: " + 转文本(表.长度())

设 表单内容 = 输入框("text", "用户名", "", "请输入用户名") + 输入框("password", "密码") + 按钮("登录")
输出 "表单长度: " + 转文本(表单内容.长度())

设 样式 = CSS类(".red", {color: "red", "font-size": "16px"})
输出 "CSS: " + 样式
输出 ""

// === 测试 应用目录 ===
导入 "应用目录"
输出 "=== 应用目录库 ==="
输出 "用户主目录: " + 用户主目录()
输出 "数据目录: " + 数据目录()
输出 "配置目录: " + 配置目录()
输出 "缓存目录: " + 缓存目录()
输出 "临时目录: " + 临时目录()
输出 "桌面目录: " + 桌面目录()
输出 "文档目录: " + 文档目录()
输出 "下载目录: " + 下载目录()
输出 "是Windows: " + 转文本(是Windows())
输出 ""

// === 测试 单调时间 ===
导入 "单调时间"
输出 "=== 单调时间库 ==="
设 器 = 创建计时器()
设 i = 0
循环 当 i < 1000:
    i = i + 1
输出 "1000次循环耗时: " + 格式化耗时(读取纳秒(器))
输出 "格式化测试: " + 格式化耗时(500) + ", " + 格式化耗时(500000) + ", " + 格式化耗时(500000000)
输出 ""

// === 测试 浮点格式化 ===
导入 "浮点格式化"
输出 "=== 浮点格式化库 ==="
输出 "定点: " + 定点格式(3.14159, 2)
输出 "科学: " + 科学格式(12345.6789, 2)
输出 "百分号: " + 百分号格式(0.314, 1)
输出 "货币: " + 货币格式(1234567.891)
输出 "千分位: " + 千分位格式(9876543.21)
输出 ""

// === 测试 模式匹配 ===
导入 "模式匹配"
输出 "=== 模式匹配库 ==="
设 选项1 = 有值(42)
设 选项2 = 无值()
输出 "选项1是有值: " + 转文本(是有值(选项1))
输出 "选项2是无值: " + 转文本(是无值(选项2))
输出 "选项1获取值: " + 转文本(获取值(选项1))
输出 "选项2默认值: " + 转文本(获取默认值(选项2, 0))

设 结果1 = 成功结果("成功!")
设 结果2 = 失败结果("出错了")
输出 "结果1是成功: " + 转文本(是成功(结果1))
输出 "结果2是失败: " + 转文本(是失败(结果2))
输出 ""

// === 测试 安全访问 ===
导入 "安全访问"
输出 "=== 安全访问库 ==="
设 数据 = {用户: {名称: "张三", 年龄: 25}}
输出 "链式获取: " + 转文本(安全链式获取(数据, ["用户", "名称"], "未知"))
输出 "链式获取(不存在): " + 转文本(安全链式获取(数据, ["用户", "地址", "城市"], "未知"))
输出 "空值检查: " + 转文本(空值检查(空))
输出 "安全转整数: " + 转文本(安全转整数("abc", -1))
输出 "安全列表访问: " + 转文本(安全列表访问([10, 20, 30], 1, -1))
输出 "安全列表访问(越界): " + 转文本(安全列表访问([10, 20, 30], 5, -1))
输出 ""

// === 测试 编码转换 ===
导入 "编码转换"
输出 "=== 编码转换库 ==="
设 原文 = "Hello 世界!"
设 编码后 = URL编码(原文)
设 解码后 = URL解码(编码后)
输出 "URL编码: " + 编码后
输出 "URL解码: " + 解码后
输出 "HTML编码: " + HTML编码("<script>alert('xss')</script>")
输出 "HTML解码: " + HTML解码("&lt;script&gt;")
输出 ""

// === 测试 发行版检测 ===
导入 "发行版检测"
输出 "=== 发行版检测库 ==="
设 信息 = 检测发行版()
输出 "发行版: " + 信息.获取默认值("名称", "未知")
输出 "版本: " + 信息.获取默认值("版本", "未知")
输出 "包管理器: " + 信息.获取默认值("包管理器", "未知")
输出 ""

// === 测试 操作系统错误 ===
导入 "操作系统错误"
输出 "=== 操作系统错误库 ==="
输出 "错误码2消息: " + 错误消息(2)
输出 "是错误码(0): " + 转文本(是错误码(0))
输出 "是错误码(1): " + 转文本(是错误码(1))
输出 ""

// === 测试 动态库加载 ===
导入 "动态库加载"
输出 "=== 动态库加载库 ==="
输出 "库后缀: " + 库后缀()
设 候选 = 库候选名("zlib")
输出 "zlib候选: " + 转文本(候选)
输出 ""

// === 测试 符号链接 ===
导入 "符号链接"
输出 "=== 符号链接库 ==="
输出 "符号链接检测测试完成"
输出 ""

// === 测试 退出过程 ===
导入 "退出过程"
输出 "=== 退出过程库 ==="
输出 "当前退出码: " + 转文本(获取退出码())
输出 "退出过程库加载成功"
输出 ""

// === 测试 HTTP服务器 ===
导入 "HTTP服务器"
输出 "=== HTTP服务器库 ==="
设 码表 = HTTP状态码()
输出 "状态码200: " + 转文本(码表.获取默认值("OK", 0))
设 响应 = JSON响应({消息: "Hello"})
输出 "JSON响应: " + 响应.主体
设 参数 = 解析URL参数("?name=语程&version=1")
输出 "URL参数: name=" + 转文本(参数.获取默认值("name", "")) + ", version=" + 转文本(参数.获取默认值("version", ""))
输出 "MIME类型(.html): " + MIME类型(".html")
输出 ""

输出 "=== 所有新库测试完成! ==="
