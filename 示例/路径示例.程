// 路径示例 - 文件路径操作工具
导入 "库/路径.程"

// === 路径合并与分割 ===
输出 "=== 路径合并与分割 ==="
设 合并结果 = 合并路径("home/user", "documents")
输出 "合并路径(\"home/user\", \"documents\"): " + 合并结果

设 分割1 = 分割路径("home/user/documents/file.txt")
输出 "分割路径(\"home/user/documents/file.txt\"): "
输出 "  目录: " + 分割1[0]
输出 "  文件: " + 分割1[1]

设 分割2 = 分割文件名("home/user/documents/file.txt")
输出 "分割文件名(\"home/user/documents/file.txt\"): "
输出 "  目录: " + 分割2[0]
输出 "  主名: " + 分割2[1]
输出 "  扩展: " + 分割2[2]

// === 路径提取 ===
输出 ""
输出 "=== 路径提取 ==="
设 测试路径 = "home/user/docs/report.pdf"
输出 "测试路径: " + 测试路径
输出 "提取目录: " + 提取目录(测试路径)
输出 "提取文件名: " + 提取文件名(测试路径)
输出 "提取扩展名: " + 提取扩展名(测试路径)
输出 "提取主文件名: " + 提取主文件名(测试路径)

// === 路径修改 ===
输出 ""
输出 "=== 路径修改 ==="
设 修改结果 = 修改扩展名("docs/report.pdf", ".txt")
输出 "修改扩展名(\"report.pdf\"→\".txt\"): " + 修改结果

设 添加结果 = 添加扩展名("docs/report", ".pdf")
输出 "添加扩展名(\"report\"→\".pdf\"): " + 添加结果

设 已有扩展 = 添加扩展名("docs/report.pdf", ".txt")
输出 "添加扩展名(已有.pdf): " + 已有扩展

// === 路径判断 ===
输出 ""
输出 "=== 路径判断 ==="
输出 "是绝对路径(\"/usr/bin\"): " + 转文本(是绝对路径("/usr/bin"))
输出 "是绝对路径(\"C:\\Windows\"): " + 转文本(是绝对路径("C:\\Windows"))
输出 "是绝对路径(\"docs/file.txt\"): " + 转文本(是绝对路径("docs/file.txt"))

// === 父目录 ===
输出 ""
输出 "=== 父目录 ==="
输出 "父目录(\"home/user/docs\"): " + 父目录("home/user/docs")
输出 "父目录(\"home/user/docs/file.txt\"): " + 父目录("home/user/docs/file.txt")

// === 路径规范化 ===
输出 ""
输出 "=== 路径规范化 ==="
输出 "规范化(\"home\\\\user\\\\docs\"): " + 路径规范化("home\\user\\docs")
输出 "规范化(\"home/user/docs\"): " + 路径规范化("home/user/docs")

输出 ""
输出 "路径示例完成！"
输出 ""
输出 "=== 示例完成 ==="
输出 ""
