// 性能分析示例 - 性能计时与统计
导入 "库/性能分析.程"

输出 "=== 计时器 ==="
设 计时器 = 创建计时器()
开始计时(计时器)
设 总 = 0
设 i = 0
循环 当 i < 100000:
    总 = 总 + i
    i = i + 1
停止计时(计时器)
输出 "10万次加法耗时: " + 格式化时间(获取经过秒(计时器))
输出 "毫秒: " + 转文本(获取经过毫秒(计时器))

输出 ""
输出 "=== 基准测试 ==="
设 结果 = 基准测试(函数(): 返回 1 + 1, 0.5, 1000000)
输出 "执行次数: " + 转文本(结果.执行次数)
输出 "总时间: " + 格式化时间(结果.总时间秒)
输出 "平均时间: " + 格式化时间(结果.平均秒)
输出 "每秒操作: " + 转文本(结果.每秒操作)
输出 ""
输出 "=== 示例完成 ==="
输出 ""